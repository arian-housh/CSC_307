{
  "name": "csc_307",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "all": "concurrently -k \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev": "npm --prefix packages/react-frontend run dev",
    "dev:frontend": "npm --prefix packages/react-frontend run dev",
    "dev:backend": "npm --prefix packages/express-backend run dev"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "concurrently": "^9.0.1",
    "nodemon": "^3.1.7"
  }
}
